# =================================
# VoiVerse AI Interview System v2.0
# =================================

# -----------------------------------
# Gemini API Configuration (Required)
# -----------------------------------

# Your Gemini API key from Google AI Studio
# Get one at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini API Base URL (Optional, for proxy or custom endpoint)
# Default: https://generativelanguage.googleapis.com/v1beta
# If using a proxy, change to your proxy address, e.g.:
# GEMINI_BASE_URL=https://your-proxy.com/v1beta
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# Model Configuration (DO NOT CHANGE unless you know what you're doing)
INTERVIEWER_MODEL=gemini-3-flash-preview
EVALUATOR_MODEL=gemini-3-pro-preview

# -----------------------------------
# HR Admin Authentication (Optional)
# -----------------------------------

# If HR_PASSWORD is not set, authentication is disabled (development mode)
# For production, always set a strong password
HR_USERNAME=admin
HR_PASSWORD=your_secure_password_here

# -----------------------------------
# Feishu Notification (Optional)
# -----------------------------------

# Feishu Webhook URL for sending interview results
# You can also configure this per job in the HR admin panel
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your_webhook_id

# -----------------------------------
# Application Settings
# -----------------------------------

# Application base URL (used for generating interview links)
# For Docker deployment, change to your server URL
# If using Nginx reverse proxy, use your domain: https://your-domain.com
APP_BASE_URL=http://localhost:8501

# Maximum interview turns before auto-ending
MAX_INTERVIEW_TURNS=28

# AI thinking level: LOW, MEDIUM, HIGH
# Higher levels provide more thorough responses but cost more
DEFAULT_THINKING_LEVEL=MEDIUM

# -----------------------------------
# Storage Configuration
# -----------------------------------

# Directory for storing interview records
# Default: ./data/interviews
DATA_DIR=./data/interviews

# -----------------------------------
# Nginx Reverse Proxy (Optional)
# -----------------------------------

# Set to "true" to enable Nginx reverse proxy in docker-compose
# When enabled, access the app via port 80/443 instead of 8501
ENABLE_NGINX=false

# Your domain name (required if ENABLE_NGINX=true)
# Used for Nginx server_name and SSL certificate
DOMAIN_NAME=your-domain.com

# Enable HTTPS with Let's Encrypt (requires valid domain)
# Set to "true" to auto-generate SSL certificates
ENABLE_SSL=false

# Email for Let's Encrypt certificate notifications
SSL_EMAIL=admin@your-domain.com

# -----------------------------------
# Docker Deployment Notes
# -----------------------------------
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in GEMINI_API_KEY with your actual API key
# 3. Set HR_PASSWORD for production security
# 4. Update APP_BASE_URL to your domain/IP
# 5. Run: docker-compose up -d
#
# For production with Nginx:
# 1. Set ENABLE_NGINX=true
# 2. Set DOMAIN_NAME to your domain
# 3. (Optional) Set ENABLE_SSL=true for HTTPS
# 4. Run: docker-compose --profile nginx up -d
